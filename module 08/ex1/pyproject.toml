[tool.poetry]
name = "python-module-08-ex1"
version = "1.0.0"
description = ""
authors  = ["gbertoli"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.1.0"
numpy = "^1.25.0"
matplotlib = "^3.7.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
